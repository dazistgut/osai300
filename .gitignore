# Secrets — never commit
*.env
.env
.env.*
*.key
*.pem
secrets.*
credentials.*
*token*
*apikey*

# Local working files
scratch/
.scratch/
.notes/
*.local.md

# Python
__pycache__/
*.pyc
.venv/
venv/
.python-version

# Node / promptfoo / pyrit caches
node_modules/
.promptfoo/
results/
cache/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
CLAUDE.md
.claude/

# Captured artifacts (allow tracking explicitly if needed)
*.har
*.pcap
*.bin

# Recon target lists — stay local, never committed (anywhere in repo).
# iplist.example.txt is the tracked template; the real iplist.txt is not.
iplist.txt
iplist.local.txt
targets.txt
targets.local.txt
